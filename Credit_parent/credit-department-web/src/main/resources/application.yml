spring:
  security:
    
  resources:
    static-locations: classpath:/static/,classpath:/view/,classpath:/public,classpath:/resources,classpath:/META-INF/resources
  thymeleaf:
    cache: false
  mvc:
    static-path-pattern: /static/**
  dubbo:
    application:
      name: credit-department
    registry:
      address: zookeeper://192.168.25.128
      port: 2181
    scan: com.credit.departmentweb.controller
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 500MB
server:
  port: 9092 #Tomcat端口    
    